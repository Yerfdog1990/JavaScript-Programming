// Variable declaration without assignment (undefined value)
let carName;

// Variable assignment
carName = "Volvo";

// Variable declaration with assignment
let person = "John Doe", carBrand = "Volvo", price = 200;

// Arithmetic operation in variable assignment
let sum1 = 5 + 2 + 3; // Simple addition
let concatenatedStrings = "John" + " " + "Doe"; // String concatenation
let mixed1 = "5" + 2 + 3; // Concatenates to "523" (string)
let mixed2 = 2 + 3 + "5"; // Adds numbers first, then concatenates to "55"

// Declaring variables with $ and _
let $myMoney = 500;
let _lastName = "Johnson";
let $$$ = 2;
let _x = 2;

// Outputting a variable value to an HTML element
document.getElementById("demo").innerHTML = carName; // Make sure an element with id "demo" exists